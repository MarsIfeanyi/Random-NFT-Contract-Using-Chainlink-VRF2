import { ethers } from "hardhat";
import {
  UNISWAPv2_CONTRACT_ADDRESS,
  USDC_UNI_HOLDER,
  ADDRESS_TO,
  UNI_ADDRESS,
  USDC_ADDRESS,
  USDC_HOLDER,
} from "../contractAddress/address";

async function main() {}

main().catch((error) => {
  console.error(error);
  process.exitCode = 1;
});
